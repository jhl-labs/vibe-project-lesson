# Project Context

> AI 에이전트가 프로젝트를 이해하기 위한 컨텍스트 정보

## 프로젝트 개요

### 기본 정보

- **프로젝트명**: `<project-name>`
- **버전**: `0.1.0`
- **상태**: Development
- **팀**: `<your-team-name>`

### 프로젝트 목적

```
<프로젝트의 목적과 해결하려는 문제를 설명>
```

### 대상 사용자

```
<주요 사용자 그룹과 그들의 요구사항>
```

## 기술 스택

### 핵심 기술

| 영역 | 기술 | 버전 | 비고 |
|------|------|------|------|
| 언어 | `<language>` | `<version>` | |
| 프레임워크 | `<framework>` | `<version>` | |
| 데이터베이스 | `<database>` | `<version>` | |
| 캐시 | `<cache>` | `<version>` | Optional |
| 메시지 큐 | `<queue>` | `<version>` | Optional |

### 개발 도구

| 도구 | 용도 |
|------|------|
| `<linter>` | 코드 품질 |
| `<formatter>` | 코드 포맷팅 |
| `<test-framework>` | 테스트 |
| Docker | 컨테이너화 |

## 아키텍처 개요

### 시스템 구성

```
┌─────────────────────────────────────────────┐
│                  Client                      │
└─────────────────┬───────────────────────────┘
                  │
┌─────────────────▼───────────────────────────┐
│              API Gateway                     │
└─────────────────┬───────────────────────────┘
                  │
┌─────────────────▼───────────────────────────┐
│            Application Layer                 │
├─────────────────────────────────────────────┤
│  ┌─────────┐  ┌─────────┐  ┌─────────┐     │
│  │Service A│  │Service B│  │Service C│     │
│  └────┬────┘  └────┬────┘  └────┬────┘     │
└───────┼────────────┼────────────┼───────────┘
        │            │            │
┌───────▼────────────▼────────────▼───────────┐
│              Data Layer                      │
│  ┌────────┐  ┌────────┐  ┌────────┐        │
│  │   DB   │  │  Cache │  │  Queue │        │
│  └────────┘  └────────┘  └────────┘        │
└─────────────────────────────────────────────┘
```

### 핵심 모듈

| 모듈 | 설명 | 주요 파일 |
|------|------|----------|
| `<module-1>` | `<description>` | `src/<path>` |
| `<module-2>` | `<description>` | `src/<path>` |
| `<module-3>` | `<description>` | `src/<path>` |

## 도메인 지식

### 핵심 개념

| 용어 | 정의 |
|------|------|
| `<term-1>` | `<definition>` |
| `<term-2>` | `<definition>` |
| `<term-3>` | `<definition>` |

### 비즈니스 규칙

1. **규칙 1**: `<description>`
2. **규칙 2**: `<description>`
3. **규칙 3**: `<description>`

## 현재 상태

### 완료된 기능

- [ ] 기능 1
- [ ] 기능 2

### 진행 중인 작업

- [ ] 작업 1
- [ ] 작업 2

### 알려진 이슈

- Issue #1: `<description>`
- Issue #2: `<description>`

## 외부 연동

### API 연동

| 서비스 | 용도 | 문서 |
|--------|------|------|
| `<service>` | `<purpose>` | `<doc-link>` |

### 인증/인가

```
<인증 방식 설명: JWT, OAuth, API Key 등>
```

## 환경 정보

### 환경 구분

| 환경 | URL | 용도 |
|------|-----|------|
| Local | localhost:3000 | 개발 |
| Dev | dev.example.com | 개발 테스트 |
| Staging | staging.example.com | QA |
| Production | example.com | 운영 |

### 필수 환경 변수

```bash
# 데이터베이스
DATABASE_URL=

# 외부 서비스
API_KEY=

# 기타
NODE_ENV=
```

## 참고 자료

- [아키텍처 결정 기록](../docs/adr/)
- [API 문서](../docs/api/)
- [개발 가이드](../docs/guides/)

---

**마지막 업데이트**: `<date>`
**담당자**: `<maintainer>`
